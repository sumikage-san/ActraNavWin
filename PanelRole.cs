namespace ActraNavWin
{
    /// <summary>
    /// パネルの役割定義。
    /// 画面上の配置位置と機能的な役割を表す。
    /// config.json の panels キー（小文字）と対応する。
    /// </summary>
    public enum PanelRole
    {
        Left,
        Center,
        Right
    }
}
