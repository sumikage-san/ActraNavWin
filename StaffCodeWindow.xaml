<Window x:Class="ActraNavWin.StaffCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ActraNavWin - ログイン"
        Height="200" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Staff Code" FontSize="14" FontWeight="SemiBold" />

        <TextBox x:Name="txtStaffCode" Grid.Row="2"
                 FontSize="14" Padding="6,4"
                 MaxLength="20" />

        <Button x:Name="btnConfirm" Grid.Row="4" Content="確定"
                Width="100" Height="32"
                HorizontalAlignment="Center"
                Click="BtnConfirm_Click"
                IsDefault="True" />
    </Grid>
</Window>
